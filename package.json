{
  "name": "cohoreasource.app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "sass --watch public/scss:public/css & http-server -p 8000 ./public",
    "deploy-functions-dev": "firebase use dev; firebase deploy --only functions",
    "deploy-functions-prod": "firebase use default; firebase deploy --only functions"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.12.3"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.51.0",
    "@typescript-eslint/parser": "^5.51.0",
    "eslint": "^8.34.0",
    "typescript": "^4.9.5"
  }
}
